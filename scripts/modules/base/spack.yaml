spack:
  include:
  - packages.yaml
  - compilers.yaml
  - modules.yaml
  - config.yaml
  specs:
  - gcc@8.4.0
  - environment-modules
  view: false
  packages:
    all:
      variants: [build_type=Release]
    gcc:
      variants: [~bootstrap ~profiled +strip]
