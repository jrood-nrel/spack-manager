spack:
  include:
  - packages.yaml
  - compilers.yaml
  - modules.yaml
  - config.yaml
  specs:
  - gcc
  - environment-modules
  view: false
  packages:
    all:
      variants: [build_type=Release]
    gcc:
      variants: [~bootstrap ~profiled +strip]
